# OpenRouter API Configuration
# Copy this to your .env file in the root directory

OPENROUTER_API_KEY=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

# MongoDB Configuration (if using MongoDB Atlas)
MONGODB_URI=mongodb://localhost:27017/learning-platform

# Server Configuration
PORT=5000
NODE_ENV=development

# JWT Secret (generate a secure random string)
JWT_SECRET=your-jwt-secret-here

# Instructions:
# 1. Create a .env file in the root directory (d:/hackathon2/)
# 2. Copy the above content to the .env file
# 3. The OpenRouter API key is already configured with your provided key
# 4. Update other values as needed for your environment
